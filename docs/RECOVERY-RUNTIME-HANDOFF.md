# Governed recovery clarification runtime candidate

This increment implements the owner-approved Section2 recovery clarification policy in the exact infra review snapshot `4d1454f85df7e3de4012b8ef8bc9c0c4045f3393`, SHA256 `173132b8d5a16d3d5a8ccdbc7355a631f8384634945772993200a3559c89da72` (SEC235 comment7-1321). It preserves approved recovery-selection/detailed-playbook artifacts, message/URL contracts and all activation defaults. It does not finish provider qualification, a deployed endpoint or mobile end-to-end release acceptance.

## Source boundaries

- `recovery_consumer.app.lambda_handler`: authenticated prepare/submit/contentless reconcile; existing account/device/deletion/entitlement/attempt gates, dedicated aggregate provider circuit budget.
- `recovery_evaluator.app.lambda_handler`: private recovery-only request, repeated projection validation, closed classifier output, deterministic approved exposure-to-action grounding. No table, other Lambda, URL fetching, generated advice or free-form provider output.
- `shared_check_authority`: new `recovery-payload-v1` HMAC domain and transport/version scope; complete-only exact-once admission and settlement. Validate transient results, then store **only** `recovery_usage` summary, never narrative/exposure/action IDs. Existing general lease worker projects the same usage-only failure when expiring recovery leases. Existing URL/message settlement and proof domains remain unchanged.
- `message_evaluator.proposer` is reused only as a fixed-host HTTPS transport with explicitly supplied recovery builder/parser. No message model qualification, default model, prompt or policy is inherited.

The public contract is `contracts/recovery-consumer/1.0.0-candidate.1` (published pin `0528bfeb96508e14c3bc3535df82f62f15a24188`). A fresh completed response may contain ephemeral IDs only after authoritative settlement; replay/reconcile returns usage only. Lost-response billing is explicitly approved. Complete complimentary receipts can record zero charged checks; current access is not historical billing evidence. Unknown accounting is never represented as zero.

## Disabled deployment interface

Both packages target Python3.14/ARM64. Terraform handlers must use the package-qualified names above; root `app.py` is also a shim. Consumer29seconds, evaluator23seconds; requested internal evaluation at most18000ms. Provider transport is stricter:250–8000ms and128–512outputtokens, one HTTP attempt. SDK secret discovery/DNS is not a mathematically guaranteed hard total deadline; post-call remaining checks and Lambda timeout remain required. Local character/wire limits are not a provider input-token guarantee.

Both: `STAGE=dev`, `RECOVERY_POLICY_VERSION=recovery-clarification-2026-09-21-v1`, `RECOVERY_POLICY_APPROVAL_SHA256=173132b8d5a16d3d5a8ccdbc7355a631f8384634945772993200a3559c89da72`, `RECOVERY_PLAYBOOK_VERSION=recovery-playbook-1.0`.

Consumer remains `RECOVERY_CONSUMER_ENABLED=false`, `AUTHORITY_ENABLED=false`, `RECOVERY_PROVIDER_CIRCUIT_OPEN=true`. Before any future activation it also needs the existing shared-authority table/keyring/Cognito/retention settings, **`DEV_SUBJECT_ALLOWLIST_JSON`** engineering allowlist, `RECOVERY_EVALUATOR_FUNCTION_ARN` fixed Dev live alias, and positive explicit `RECOVERY_PROVIDER_WINDOW_SECONDS`, `RECOVERY_PROVIDER_ATTEMPTS_PER_WINDOW`, `RECOVERY_PROVIDER_FAILURES_PER_WINDOW`. Disabled bootstrap requires none of the omitted settings or credentials. Shared HMAC access remains AWSCURRENT; no new secret/table is created.

Evaluator remains `RECOVERY_EVALUATOR_ENABLED=false`, `RECOVERY_AI_ENABLED=false`, `RECOVERY_AI_QUALIFIED=false`. Future activation additionally needs `RECOVERY_AI_MODEL` exact dated snapshot, `RECOVERY_AI_SECRET_ARN` approved Dev OpenAI secret, `RECOVERY_AI_QUALIFICATION_ID`, `RECOVERY_AI_TIMEOUT_MS`, `RECOVERY_AI_MAX_OUTPUT_TOKENS`, and separately reviewed secret permission. No environment defaults select a model. Its qualification registry is empty. A qualification binds policy approval, immutable bundle, prompt/schema, exact executable/request-template profile, model and timeout/output limits. Evidence-file hash matching proves identity, not quality by itself; real independently reviewed evidence is still necessary before adding an entry.

Requests use the official fixed `https://api.openai.com/v1/responses` route, strict structured outputs, no tools/background/streaming, store:false, default service tier, disabled truncation. Parsing requires a completed single assistant text result from the exact model snapshot. Unknown/refusal/incomplete/reasoning envelopes fail closed. Model compatibility must be demonstrated separately; none is claimed. store:false is not a guarantee of zero provider retention. Provider project access/data handling/pricing remain activation evidence. Official references: https://developers.openai.com/api/reference/python/resources/responses/methods/create and https://developers.openai.com/api/docs/guides/structured-outputs .

## Qualification and remaining acceptance

`evaluation/recovery_ai/development-proposals.json` contains28synthetic EN/ES proposed-label cases (14translation families), all engineering-only development material, no independent reviewers or held-out claim. It covers affirmative self exposure, combined exposures, third-person accounts, scammer demands/quotes, negation, hypotheticals, unclear attribution, redacted context and injection. The runtime fixtures/tests inject outputs to verify contracts and accounting; they do not measure a model’s semantic compliance.

Before qualification: independently review labels and permitted provenance; add ambiguous/mixed/contradictory and unsupported-language hard negatives, secret/placeholder boundaries, long Unicode and adversarial variants; partition by translation/source family without leakage; freeze model/prompt/schema/source/request-profile/limits; approve numerical semantic quality and per-language false-positive/miss/abstention targets before held-out measurement. Separately measure compatibility, truncation/refusal/error behavior, latency, provider input/cost bounds and no-retry behavior. Record denominators and distinguish deterministic guard skips, model attempts, valid outputs, actual exposure quality and final accounting. Preserve raw evaluation text outside runtime artifacts and use minimized evidence metadata/aggregates in any future approved qualification record.

Actual provider access/retention/pricing evidence, a specific recovery model, numerical quality thresholds, bounded paid-run authorization and real measured results are not supplied by this source increment. No provider call, credential retrieval, activation, deployment, or production claim is made. SEC235 and ATCR121 retain those acceptance gates, mobile integration/native lifecycle validation, and deployed engineering end-to-end checks.

## Validation scope

Focused tests exercise fake HTTP framing through the real recovery builder/parser, no redirects/retries, fixed-host projection, malformed outputs, privacy checks before credentials, original Unicode controls, exact action grounding, disabled bootstrap, absent qualification, profile identity changes, Moto account/device/allowance fences, usage-only persistence, duplicate/lost-response reconciliation, complimentary accounting, uncertain settlement and both explicit/background lease recovery. Source-only archive tests import exact Terraform handlers and lightweight old-worker dependencies; they are not ARM execution evidence. Full archive dependency assembly and final command/count/hash evidence are recorded in the PR after gates complete.
