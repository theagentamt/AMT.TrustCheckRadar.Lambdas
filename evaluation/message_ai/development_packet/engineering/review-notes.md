# Engineering ambiguity notes — do not give to blind reviewers

These are unresolved engineering questions, not adjudicated corrections. The current
`expected` category sets are proposals only. The offline exact assessment-and-reason-set
comparison must not be treated as qualified accuracy. Preserve the unhinted worksheet
until both actual reviewers finish independently.

| Cases | Question for later adjudication |
| --- | --- |
| dev-001-en/es | Refund rationale may additionally support AI_PRETEXT alongside the proposed credential request. Establish whether the text grounds that category without assuming the correspondent's real identity. |
| dev-002-en/es and dev-016-en/es | Parcel withholding/destruction consequences may additionally support AI_PRETEXT. Distinguish payment pressure and consequential urgency from the claimed situation; do not force single-label classification. |
| dev-003-en/es | Prize-office advance-payment rationale proposes pretext and payment pressure. Review whether the conditional advance requirement supplies enough pressure, rather than silently treating every payment request as pressured. |
| dev-019-en/es | Transfer request with account-freeze consequence may additionally support AI_PAYMENT_PRESSURE and/or AI_PRETEXT alongside proposed urgency. The Unicode offset example is not a validated complete category set. |
| dev-006-en/es, dev-008-en/es, dev-020-en/es | Existing qualified rules should skip AI. Proposed semantic labels are comparison proposals only; no actual AI category prediction is measured for skipped cases. |
| dev-009-en/es | Content is protective but quoted and outside the current AI profile. Keep semantic harmlessness separate from supported-scope abstention and benign-completion denominators. |
| dev-018-en/es | Benign-looking text and simulated independent threat are intentionally different stages. A text no-warning label is not a false-negative about link reputation. The backend-only fixture does not enable current mobile joint-link submission. |

For every warning, the proposed full-sentence spans should be reviewed for minimal
semantic support and negation/attribution. Broad syntactically valid spans are not
proof of correct evidence. Select separate action/pressure spans where helpful;
retain one to three ordered non-overlapping spans per category.

## Coverage still missing before a real development/qualification plan

This small packet does not exhaust the approved negative boundaries. Additional
fresh development examples should cover independent entry into a known service
versus disclosure to a correspondent, unfamiliar names/demographics/HTTP alone,
spelling variants, contradictory mixed language, all three spans across all five
categories at the output ceiling, near-limit message lengths, and blinded semantic
paraphrases from independent permitted sources. Technical malformed/refusal/truncation
and deadline fixtures already belong to the separate runner tests; do not count those
as independent human-labeled semantic samples.

No reviewer recruitment, rights review, actual bilingual semantic review, adjudication,
representative sampling or untouched holdout has occurred. Completion of structural
checks changes none of those facts.
