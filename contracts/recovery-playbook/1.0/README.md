# Approved detailed recovery playbook 1.0

Owner instruction “Approve the detailed built-in playbooks” approved Section 1 of
the exact review at commit 4d1454f85df7e3de4012b8ef8bc9c0c4045f3393, SHA-256
173132b8d5a16d3d5a8ccdbc7355a631f8384634945772993200a3559c89da72.
Tracker records SEC163 7-1318, SEC234 7-1319, ATCR121 7-1320 bind that approval.

This immutable self-contained bundle includes nine actions, their phases/ranks,
four category limits, two bilingual phase headings, the exact conditional-device
note, existing common introduction/category titles/official-help copy and fixed
HTTPS links. Action/limit text is verbatim from the previous approved snapshot;
splitting/grouping and the replacement conditional note have explicit approval.
Original basic-content and selector artifacts are preserved unchanged.

Input is the approved recovery-selection-1.0.0 structured exposure selection.
Union selected actions; add device actions for link selection; sort now before
follow_up, then numeric rank; deduplicate action IDs. Keep applicable limits once
beside relevant guidance; never omit the introduction or official-help section.
All 32 subsets and unsure have exact EN/ES expected fixtures (66 total). Empty or
unsure means all categories. Show-all empties selections. Show the conditional
note for link-only dependencies AND every empty/unsure/show-all fallback. Omit it
only if device access was explicitly selected in a non-fallback plan.

Ranks/groups are presentation guidance, not individualized triage or assurances
that delay is safe. No inference of compromise, download, loss or recovery is made.

The new bundle's approval date is 2026-09-21; its own 180-day review date is
2027-03-20. After that date (or a clock preceding approval), retain text with the
existing review-due notice. The original basics keeps its 2026-09-20 date. Require
the current authenticated session, without extending expired sessions or creating
a new offline-login exception. Subscription/allowance does not restrict local help.
Selections stay volatile and clear on exit/background/account change; no incident
storage, analytics, upload, model, automatic links or billing operation is added.

A valid detailed bundle is independently complete and remains available even when
the older basic bundle is withdrawn. If detailed content is missing, withdrawn,
incompatible or modified, fall back only to an independently Approved compatible
basic bundle under its own date/session rules. If both are invalid/withdrawn, show
limited help without invented instructions. If shared content is unsafe, withdraw
BOTH affected versions. Distribution remains app-update-only; offline installs
cannot discover new withdrawals until updated. No instant revocation claim.

`fallbackPlan` describes the old approved local plan; detailed action arrays stay
empty in that state. The fallback UI must use that baseline's common content and
review metadata, not mix active versions. The exact static official destinations
open only after explicit confirmation, with no incident/account/query parameters.

Run `python -m pytest -q tests/recovery_playbook tests/recovery_selection`.
Tests verify exact wording, default availability, ordering/permutations, note rules,
three withdrawal combinations and independent review-date boundaries. Android
owns rendering/lifecycle/device evidence. The detailed bundle does not implement
the separately approved optional AI service or claim model quality/activation.
