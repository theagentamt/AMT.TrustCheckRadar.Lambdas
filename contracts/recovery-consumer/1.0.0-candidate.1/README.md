# Governed recovery clarification transport candidate 1

Owner-approved semantic policy: `recovery-clarification-2026-09-21-v1`, approval snapshot SHA256 `173132b8d5a16d3d5a8ccdbc7355a631f8384634945772993200a3559c89da72` (infra source `4d1454f85df7e3de4012b8ef8bc9c0c4045f3393`, SEC235 comment 7-1321). This transport is implemented behind disabled candidate gates. It does not authorize a model, paid call, endpoint deployment or production use.

Routes are POST `/v1/recovery-clarifications/prepare`, `/v1/recovery-clarifications`, and `/v1/recovery-clarifications/reconcile`. Account/JWT and active-device rules reuse the shared authority. New processing requires external-check entitlement. Basic local help remains independent of these endpoints.

Prepare binds exact reviewed text, declarations, language, public check ID, policy and playbook version to an opaque operation proof; no allowance is reserved until submit admission. Submit uses that exact intent and proof. No automatic model retry or result regeneration occurs. Reconcile sends only transport version, check ID and proof. A changed identity requires a fresh preview and distinct check; never silently rewrite after review.

A first fresh settled response can return a closed, grounded `outcome`, `usage`, and `resultAvailability: available`. Only validated nonempty complete suggestions are chargeable, once. Other outcomes are nonchargeable. Complimentary accounts can have complete outcomes with zero charged checks. The UI must apply exposure IDs only after explicit confirmation and fence by account/device/request generation/bundle version. Render only approved playbook actions, never provider prose.

The ledger retains only `usage` and existing accounting metadata for seven days, not descriptions, exposure IDs or action IDs. Any submit replay, prepare replay after settlement, or reconciliation returns `outcome: null` and `resultAvailability: not_retained` with authoritative usage. A lost complete response can therefore count even though suggestions cannot be recovered. Never promise zero charge from absence of suggestions. Unknown/pending accounting uses null charged checks and reconciliation required.

Prepared and certainly undispatched requests reserve no allowance and can be discarded. After dispatch may have occurred, cancellation preserves only a minimal receipt for contentless reconciliation; local help survives. Never persist narratives, original/cleaned descriptions, unapplied suggestions or model output in saved state, history, logs or an outbox.

HTTP statuses: 200 prepared/settled/reconciled-not-started; 202 pending; 401 authentication required; 403 account/device/external access/allowance; 409 identity conflict or expired operation; 422 rejected projection/unsupported contract; 429 security rate limit; 503 unavailable/disabled. Error envelopes conservatively retain unknown accounting. Server security-attempt counters may increment on rejected authenticated input; no check preparation/admission/provider processing occurs before privacy validation. Client code must validate envelope state/accounting coherence, exact identity, and usage/outcome versions rather than infer billing from HTTP status.

`projection-profile.json` and synthetic `projection-fixtures.json` supplement the nine-type local sanitizer. This is bounded recognized-format minimization, not comprehensive anonymization; raw names and contextual identifying details are not reliably detected. The backend validates exact NFC text, controls, token declarations/reuse, residual identifiers, explicit labeled secrets and IPv6 independently. Backend limits are cleaned 2000 code points/8000 UTF8 bytes/32768 wire bytes; original 2000-code-point input can only be enforced on device. No manual category selections/baseline/account identifiers enter the model projection.

`response-fixtures.json` exercises protocol states only. Model labels are injected simulations, not model compliance, accuracy, semantic grounding or bilingual qualification evidence. Runtime complete means a structurally valid grounded category suggestion under a separately qualified classifier, not verified real-world exposure or recovery success. The qualification registry is empty.
